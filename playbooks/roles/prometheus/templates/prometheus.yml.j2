global:
  scrape_interval: {{ services.monitoring.prometheus.scrape_interval }}

scrape_configs:
  - job_name: 'node'
    static_configs:
      - targets:
{% for t in services.monitoring.prometheus.targets %}
          - '{{ t }}'
{% endfor %}
